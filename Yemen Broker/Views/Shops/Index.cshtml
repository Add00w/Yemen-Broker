@model IEnumerable<Yemen_Broker.Models.Shop>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ad.City.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ad.AdDescribtion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ad.AdPrice)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumberOfDoors)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StreetArea)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ad.City.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ad.AdDescribtion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ad.AdPrice)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumberOfDoors)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StreetArea)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.AdId }) |
            @Html.ActionLink("Details", "Details", new { id = item.AdId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.AdId })
        </td>
    </tr>
}

</table>
